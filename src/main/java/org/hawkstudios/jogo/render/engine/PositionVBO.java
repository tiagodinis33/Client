package org.hawkstudios.jogo.render.engine;

import org.hawkstudios.jogo.render.internal.VBO;

public class PositionVBO extends VBO {
    public PositionVBO(int slot, int vectorSize){
        super(slot, vectorSize);
    }
}